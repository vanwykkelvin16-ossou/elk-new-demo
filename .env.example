ADMIN_BOOTSTRAP_KEY=
# Supply a unique private setup code through Sites runtime secrets.
# Only enable behind the Sites dispatcher, which verifies and supplies identity headers.
# Leave unset on standalone deployments; email/password accounts still work.
TRUST_SITES_IDENTITY=
