; @lovable.dev/vite-tanstack-config declares a strict nitro peer range that
; conflicts under npm's default resolver; the stack is known-good, so allow
; installs to proceed (matches how Lovable builds this project).
legacy-peer-deps=true
